# Python beadandó – FastAPI + Streamlit + Weather API

Futtatás:
```bash
python -m venv venv
source venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
uvicorn backend.app:app --reload
streamlit run frontend/app.py
```
